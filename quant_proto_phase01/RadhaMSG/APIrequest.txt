What I need (minimal)
1) Entry points + result shapes

compile(term, materialize: bool=False) -> (circ, perm)

compile_goi(term, materialize: bool=False) -> (circ, perm) | goi_artifact

How do we distinguish extracted vs residual? (e.g., isinstance(x, GOIArtifact) or tagged union)

2) Term constructors / AST nodes

I need the concrete names to build terms:

Sequential composition: e.g. Seq(f,g) / SeqTerm(f,g)

Tensor/parallel: e.g. TenTerm(f,g)

Structural constructors: e.g. TwistTensor, AssocL, AssocR, SwapSum, etc. (at least one nontrivial structure term)

Gate constructors: e.g. H(i), S(i), CX(i,j) (or Gate("H",[i]), etc.)

Phase 3: Feedback(k, body) or Feedback_k(body)

If you have a parser from a compact syntax string, that’s even better (then tests are stable and concise).

3) Circuit “command stream” extraction

To lock down determinism and exact behavior, I need a canonical way to render circuits:

either circ.get_commands() with stable fields, or

an existing helper that already prints a canonical command list,

or I will implement a serializer if you tell me what object type circ is (pytket Circuit?).

4) WirePerm inspection

Need one of:

perm.to_list() / perm.as_dict() / perm.apply_new_to_old(i)

and width information (or inferable from term types)

5) Existing “no swaps by default” identification

How do we detect SWAP commands?

If pytket: command/op name equals "SWAP" (or OpType.SWAP)

otherwise: tell me the tag/name.

That’s it.